### Added

- Added settings and favorites export/import controls under Settings -> Data Management.
- Added fully anonymized, errors-only reporting via Sentry. Reports retain code stack and bounded error-class diagnostics while excluding user, cluster, request, device, and other runtime identifiers. You can opt out in Settings -> Data Management.

### Changed

### Fixed
