engine biome(1.0)
language js(typescript, jsx)

// Keep the combined workspace RPC and its Wails state events behind the one
// clusterWorkspaceStore owner. Feature code consumes selectors/facades instead
// of creating another cluster-keyed cache or subscription plane.
or {
  `$receiver.GetClusterWorkspaceState($...)` as $call where {
    register_diagnostic(
      span = $call,
      message = "Read cluster workspace state through clusterWorkspaceStore.",
      severity = "error"
    )
  },
  `GetClusterWorkspaceState($...)` as $call where {
    register_diagnostic(
      span = $call,
      message = "Read cluster workspace state through clusterWorkspaceStore.",
      severity = "error"
    )
  },
  `$receiver.onEvent("cluster:lifecycle", $...)` as $call where {
    register_diagnostic(span = $call, message = "Subscribe to cluster workspace state through clusterWorkspaceStore.", severity = "error")
  },
  `$receiver.onEvent("cluster:auth:failed", $...)` as $call where {
    register_diagnostic(span = $call, message = "Subscribe to cluster workspace state through clusterWorkspaceStore.", severity = "error")
  },
  `$receiver.onEvent("cluster:auth:recovering", $...)` as $call where {
    register_diagnostic(span = $call, message = "Subscribe to cluster workspace state through clusterWorkspaceStore.", severity = "error")
  },
  `$receiver.onEvent("cluster:auth:recovered", $...)` as $call where {
    register_diagnostic(span = $call, message = "Subscribe to cluster workspace state through clusterWorkspaceStore.", severity = "error")
  },
  `$receiver.onEvent("cluster:auth:progress", $...)` as $call where {
    register_diagnostic(span = $call, message = "Subscribe to cluster workspace state through clusterWorkspaceStore.", severity = "error")
  },
  `$receiver.onEvent("cluster:health:healthy", $...)` as $call where {
    register_diagnostic(span = $call, message = "Subscribe to cluster workspace state through clusterWorkspaceStore.", severity = "error")
  },
  `$receiver.onEvent("cluster:health:degraded", $...)` as $call where {
    register_diagnostic(span = $call, message = "Subscribe to cluster workspace state through clusterWorkspaceStore.", severity = "error")
  },
  `$receiver.onEvent("cluster:scope:changed", $...)` as $call where {
    register_diagnostic(span = $call, message = "Subscribe to cluster workspace state through clusterWorkspaceStore.", severity = "error")
  },
  `onEvent("cluster:lifecycle", $...)` as $call where {
    register_diagnostic(span = $call, message = "Subscribe to cluster workspace state through clusterWorkspaceStore.", severity = "error")
  },
  `onEvent("cluster:auth:failed", $...)` as $call where {
    register_diagnostic(span = $call, message = "Subscribe to cluster workspace state through clusterWorkspaceStore.", severity = "error")
  },
  `onEvent("cluster:auth:recovering", $...)` as $call where {
    register_diagnostic(span = $call, message = "Subscribe to cluster workspace state through clusterWorkspaceStore.", severity = "error")
  },
  `onEvent("cluster:auth:recovered", $...)` as $call where {
    register_diagnostic(span = $call, message = "Subscribe to cluster workspace state through clusterWorkspaceStore.", severity = "error")
  },
  `onEvent("cluster:auth:progress", $...)` as $call where {
    register_diagnostic(span = $call, message = "Subscribe to cluster workspace state through clusterWorkspaceStore.", severity = "error")
  },
  `onEvent("cluster:health:healthy", $...)` as $call where {
    register_diagnostic(span = $call, message = "Subscribe to cluster workspace state through clusterWorkspaceStore.", severity = "error")
  },
  `onEvent("cluster:health:degraded", $...)` as $call where {
    register_diagnostic(span = $call, message = "Subscribe to cluster workspace state through clusterWorkspaceStore.", severity = "error")
  },
  `onEvent("cluster:scope:changed", $...)` as $call where {
    register_diagnostic(span = $call, message = "Subscribe to cluster workspace state through clusterWorkspaceStore.", severity = "error")
  }
}
