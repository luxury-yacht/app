# Feel free to remove those if you don't want/need to use them.
# Make sure to check the documentation at https://nfpm.goreleaser.com
#
# The lines below are called `modelines`. See `:help modeline`

name: "__APP_BINARY_NAME__"
arch: ${GOARCH}
platform: "linux"
version: "__APP_VERSION__"
section: "default"
priority: "extra"
maintainer: ${GIT_COMMITTER_NAME} <${GIT_COMMITTER_EMAIL}>
description: "__APP_DESCRIPTION__"
vendor: "__APP_COMPANY__"
homepage: "https://luxury-yacht.app"
license: "GPLv3"
release: "1"

contents:
  - src: "./bin/__APP_BINARY_NAME__"
    dst: "/usr/local/bin/__APP_BINARY_NAME__"
  - src: "./build/appicon.png"
    dst: "/usr/share/icons/hicolor/128x128/apps/__APP_BINARY_NAME__.png"
  - src: "./build/linux/__APP_BINARY_NAME__.desktop"
    dst: "/usr/share/applications/__APP_BINARY_NAME__.desktop"

# Default dependencies for the GTK4 + WebKitGTK 6.0 stack (Ubuntu 24.04+ / Debian 13+)
depends:
  - libgtk-4-1
  - libwebkitgtk-6.0-4

# Distribution-specific overrides for different package formats
overrides:
  # RPM packages for Fedora / RHEL / AlmaLinux / Rocky Linux
  rpm:
    depends:
      - gtk4
      - webkitgtk6.0

  # Arch Linux packages
  archlinux:
    depends:
      - gtk4
      - webkitgtk-6.0

# scripts section to ensure desktop database is updated after install
scripts:
  postinstall: "./build/linux/nfpm/scripts/postinstall.sh"
  # You can also add preremove, postremove if needed
  # preremove: "./build/linux/nfpm/scripts/preremove.sh"
  # postremove: "./build/linux/nfpm/scripts/postremove.sh"

# replaces:
#   - foobar
# provides:
#   - bar
# recommends:
#   - whatever
# suggests:
#   - something-else
# conflicts:
#   - not-foo
#   - not-bar
# changelog: "changelog.yaml"
